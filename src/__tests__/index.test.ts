/**
 * Placeholder test — keeps the jest pre-flight green (the extension has no
 * unit-testable logic yet; the parent library owns the ordering/lifecycle
 * machinery this library consumes).
 */
import { describe, expect, test } from '@jest/globals';

describe('react-native-mapsforge-vtm-ext-grib', () => {
	test('loads', () => {
		expect(true).toBe(true);
	});
});
